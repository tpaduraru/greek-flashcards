# NT Greek Flashcards — Python dependencies
# Install with:  pip install -r requirements.txt

Flask>=3.0,<4.0          # web framework (dev server + routing)
gunicorn>=21.0           # production WSGI server (see README)
